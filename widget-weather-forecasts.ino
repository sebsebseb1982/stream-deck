void drawMeteoWidget(unsigned int column, unsigned int row, WeatherForecast weatherForecast, String label) {
    unsigned int x = column * BUTTON_SIZE;
  unsigned int y = row * BUTTON_SIZE;
  /*drawButton(
    x,
    y,
    label,
    UNAVAILABLE
  );

  screen.drawBitmap(
    x + 15,
    y + 2,
    clearSky,
    50,
    50,
    WHITE
  );
  */
}
